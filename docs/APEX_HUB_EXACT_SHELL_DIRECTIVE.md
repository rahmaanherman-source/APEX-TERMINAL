# APEX 365 — PERMANENT OWNER HUB SHELL LOCK

**Status:** LOCKED / CANONICAL / OWNER-APPROVED
**Scope:** APEX 365 owner/developer hub only.
**Authority:** Owner instruction + supplied APEX 365 reference image.

## 1. WHAT THIS IS

APEX 365 is the owner's permanent command center: the place the owner sits down on computer, phone, tablet, or other supported device to create, build, structure, inspect, connect, verify, and operate the APEX ecosystem.

This is **NOT the customer-facing product UI**.

Customer-facing applications, storefronts, consumer experiences, and public product surfaces are separate concerns and must not redefine, simplify, replace, or visually bleed into this hub.

## 2. PERMANENT SHELL LAW

The supplied APEX 365 reference is the exact visual and information-architecture source of truth for the owner hub.

The shell remains locked until the owner explicitly changes it and says otherwise.

Do not:
- redesign it
- simplify it
- reinterpret it
- turn it into a generic dashboard
- substitute a different layout
- remove panels because implementation is difficult
- move core regions without explicit owner authorization
- let customer-facing UI dictate the owner hub
- allow responsive behavior to deteriorate the canonical structure

Responsive implementation means **adapt the same shell to the device**, not redesign the shell for the device.

## 3. REQUIRED ROOT EXPERIENCE

Every entry into APEX 365 opens the same canonical owner workspace:

- APEX Terminal identity/header
- Gabby command/search surface
- Truth / verification state
- owner/project context
- Workspace navigation
- Connected Apps
- central 3D Creation Studio
- Concierge Gabby
- project status
- Engines
- System Status
- Foley & Sound Design
- AI Dialogue & ADAK
- Timeline / Sequence
- real-time Audit Feed
- bottom Build / Run / Test / Verify / Deploy / Publish controls
- APEX Real-Time Engine status
- Truth status
- Gabby online status

The root shell is the **front door**.

## 4. UNDER-DASHBOARD TABS

Everything else lives beneath the root shell and is reached through its navigation. No competing root dashboard may be created.

Required owner workspace destinations include:

Dashboard, Projects, Tools, Engines, Connections, Marketplace, Audit Log, Memory Slabs, Settings, Revenue, Gabby, Character Studio, Worlds, Animation, Render, Terminal, Vault/Gatekeeper, Truth Gate, Provider Registry, and other capabilities only when they belong to the owner hub.

## 5. FUNCTIONALITY

The shell is not a picture. Every visible control must resolve to a real navigation, state transition, configured execution, truthful disabled state, or truthful error state.

Required execution chain:

`CONTROL → ROUTE → AUTH → PERMISSION → CAPABILITY → EXECUTION → READBACK → EVIDENCE → AUDIT → UI STATE`

No dead buttons. No fake connections. No simulated success. No fake verification.

## 6. PRESERVATION

The existing APEX/GODSPEED engine and its legitimate capabilities are the functional floor.

**CHANGE THE SHELL WITHOUT LOSING THE ENGINE.**

Reuse first. Extend second. Generate last.

Do not create duplicate engines, duplicate terminals, duplicate registries, or competing owner dashboards.

## 7. TRUTH

Use:

`DOCUMENTED → CONFIGURED → CONNECTED → RUNNABLE → TESTED → VERIFIED`

A visual label never creates truth. Runtime evidence creates truth.

`REGISTERED ≠ CONNECTED`
`CONNECTED ≠ EXECUTED`
`EXECUTED ≠ VERIFIED`
`CLICKED ≠ SUCCESS`

If something cannot operate, show its real state: `NOT_CONFIGURED`, `CREDENTIAL_REQUIRED`, `PERMISSION_REQUIRED`, `SERVICE_UNAVAILABLE`, `EXECUTION_FAILED`, or `BLOCKED`.

## 8. DEVICE LAW

Computer, phone, tablet, and other supported devices are different viewports into **one APEX 365 owner hub**.

Do not create separate product identities or divergent shells for each device.

Preserve the canonical information hierarchy, panel relationships, controls, status visibility, Gabby presence, and command surface. Adapt dimensions and interaction mechanics only as required for the device.

## 9. CUSTOMER SEPARATION LAW

Customer-facing products are downstream experiences.

`APEX 365 OWNER HUB → BUILD / GOVERN / OPERATE → CUSTOMER PRODUCTS`

Customer UI must never become the APEX 365 shell.

APEX 365 is where the owner builds the structure. The customer sees the resulting product, not the owner's internal command center.

## 10. CHANGE CONTROL

This document remains locked indefinitely until the owner explicitly authorizes a shell change.

No builder, AI agent, developer, contractor, design system, responsive framework, customer product, or future implementation may override this lock by assumption.

**THIS IS APEX 365.**
**THIS IS THE OWNER HUB.**
**THIS IS THE FRONT DOOR.**
**THIS SHELL DOES NOT CHANGE UNTIL THE OWNER CHANGES IT.**
**NO APPROXIMATION. NO DETERIORATION. NO FEATURE BLEED. NO VISUAL BLEED. NO FAKE GREEN.**
